OBJS=lib:c.o AmiCron25.o timer.o

LIBS=lib:lc.lib lib:amiga.lib

NAME=AmiCron

CC=lc

CFLAGS= -csuw -dLATTICE -r -b -v

$(NAME): $(OBJS)
	Blink from $(OBJS) lib $(LIBS) VERBOSE SC SD ND to $(NAME)


AmiCron25.o: AmiCron25.c 
	$(CC) $(CFLAGS) AmiCron25.c

timer.o: timer.c 
	$(CC) $(CFLAGS) timer.c

