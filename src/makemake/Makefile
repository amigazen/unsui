#Makefile generated by Makemake (C)1994 Jeff Shepherd
CC = g++
CFLAGS = -O2 -msmall-code
LFLAGS = -s -noixemul
OBJ = makemake.o
OUTPUT = makemake

.SUFFIXES: .cc .h .o
.cc.o:
	$(CC) $(CFLAGS) -c $< -o $@

all : $(OUTPUT)

$(OUTPUT) : $(OBJ)
	$(CC) -o $(OUTPUT) $(OBJ) $(LFLAGS)

